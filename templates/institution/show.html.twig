{% extends 'base.html.twig' %}

{% block title %}Institution Details {% endblock %}

 {% block pageheader %} 
    <h1>Institution Details</h1>
 {% endblock %} 

{% block body %}

    {% if is_granted('ROLE_ADMIN') %}
    <div class='btn-toolbar pull-right'>
        <div class='btn-group'>
            <a href="{{ path('institution_edit', {'id': institution.id }) }}" class="btn btn-default">
                <span class="glyphicon glyphicon-edit"></span> Edit
            </a>
        </div>
        <div class='btn-group'>
            <form class='delete-form' method="post" action="{{ path('institution_delete', {'id': institution.id }) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
                <input type="hidden" name="_method" value="DELETE">
                <input type="hidden" name="_token" value="{{ csrf_token("delete" ~ institution.id) }}">
                <button class="btn btn-danger"><span class="glyphicon glyphicon-trash"></span> Delete</button>
            </form>
        </div>
    </div>
    {% endif %}

    <table class="table table-bordered table-condensed table-hover table-striped">
        <tbody>
            <tr>
                <th>Province</th>
                <td>{{ institution.province }}</td>
            </tr>
        <tr>
                <th>Name</th>
                <td>{{ institution.name }}</td>
            </tr>
                <tr>
            <th>Created</th>
            <td>{{ institution.created|date }}</td>
        </tr>
        <tr>
            <th>Updated</th>
            <td>{{ institution.updated|date }}</td>
        </tr>
        </tbody>
    </table>

{% endblock %}
