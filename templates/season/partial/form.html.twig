{{ form_start(form) }}
{{ form_widget(form) }}
<div class="form-group">
    <div class="col-sm-2"></div>
    <div class="col-sm-10">
        <input type="submit" value="Save" class="btn btn-primary" />
        <a href="{{ path('season_show', { 'id': season.id }) }}" class="btn btn-warning"> Cancel </a>
    </div>
</div>
{{ form_end(form) }}

