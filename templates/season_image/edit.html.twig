{% extends '@NinesMedia/image/edit.html.twig' %}

{% block breadcrumbs %}
    <li class="breadcrumb-item"><a href="{{ path('homepage') }}">Home</a></li>
    <li class="breadcrumb-item"><a href="{{ path('podcast_index') }}">Podcasts</a></li>
    <li class="breadcrumb-item"><a href="{{ path('podcast_show', {'id': entity.podcast.id}) }}">{{ entity.podcast.title }}</a></li>
    <li class="breadcrumb-item"><a href="{{ path('season_show', {'podcast_id': entity.podcast.id, 'id': entity.id}) }}">{{ entity.title }}</a></li>
    <li class="breadcrumb-item"><a href="{{ path('season_image_show', {'podcast_id': entity.podcast.id, 'season_id': entity.id, 'id': image.id}) }}">{{ image.originalName }}</a></li>
    <li class="breadcrumb-item active" aria-current="page">Edit</li>
{% endblock %}