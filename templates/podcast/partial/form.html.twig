{% form_theme form with [
    '@NinesUtil/form/bootstrap_5_layout.html.twig',
    '@TetranzSelect2Entity/Form/fields.html.twig',
    'jquery.collection.html.twig',
] %}
<h1 class="h4">{{ (podcast.id is null ? 'Create' : 'Edit') ~ ' Podcast' }}</h1>
{{ form_start(form, {
    aria_label: (podcast.id is null ? 'Create' : 'Edit') ~ ' Podcast',
}) }}
{{ form_widget(form) }}
<div class="mb-3 text-center">
    {% if podcast.id is null %}
        <input type="submit" value="Create" class="btn btn-primary" />
        <input name="submitAndContinue" type="submit" value="Save and Continue" class="btn btn-primary" />
        <a href="{{ path('podcast_index') }}" class="btn btn-warning"> Cancel </a>
    {% else %}
        <input type="submit" value="Save" class="btn btn-primary" />
        <input name="submitAndContinue" type="submit" value="Save and Continue" class="btn btn-primary" />
        <a href="{{ path('podcast_show', { id: podcast.id }) }}" class="btn btn-warning"> Cancel </a>
    {% endif %}
</div>
{{ form_end(form) }}