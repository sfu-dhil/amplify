{% extends 'base.html.twig' %}

{% block title %}New Audio {% endblock %}

{% block body %}
    <h1>New Audio</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="mb-3 row">
        <div class="col-sm-2"></div>
        <div class="col-sm-10">
            <input type="submit" value="Create" class="btn btn-primary" />
            <a href="{{ path('nines_media_audio_index') }}" class="btn btn-warning">Cancel</a>
        </div>
    </div>
    {{ form_end(form) }}

{% endblock %}
